import { createStore } from 'vuex'

import auth from './auth'
// Vue.use(Vuex)
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth
  }
})
